#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool premier(int a){
  if(a % ){
    return;
  }
  return;
}

int main(void) {

  int a;
  if(scanf("%d", &a) =! 1){
    return EXIT_FAILURE;
  }
  premier(a);
  return EXIT_SUCCESS;
}

/*
Un nombre entier strictement positif est premier si et seulement si il n’est divisible que par 1 et
par lui-même. Attention, 1 n’est pas premier. Écrivez une fonction prenant en paramètre un entier
strictement positif et retournant une valeur booléenne indiquant si cet entier est premier ou pas.
*/
